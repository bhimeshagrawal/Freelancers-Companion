<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css">
    <title>Blog - MonkeySingh</title>
</head>

<body>
    <%- include("partials/header.ejs") %>
        <!-- blog -->
        <h1 class="text-6xl mt-24 font-bold text-center">Blog</h1>
        <!-- Blog Posts -->
        <div class="overflow-x-hidden">
            <div class="px-6 py-8">
                <div class="container flex justify-between mx-auto">
                    <div class="w-full">
                        <!-- blog posts  -->
                        <% blogPostArray.forEach(blogPost=> { %>
                            <div class="mt-6">
                                <div class="max-w-4xl px-10 py-6 mx-auto bg-white rounded-lg shadow-md">
                                    <div class="flex items-center justify-between"><span
                                            class="font-light text-gray-600">
                                            <%=blogPost.date%>
                                        </span><a href="/blog/<%=blogPost._id%>"
                                            class="px-2 py-1 font-bold text-gray-100 bg-blue-600 rounded hover:bg-blue-500">
                                            <%= blogPost.category %>
                                        </a>
                                    </div>
                                    <div class="mt-2"><a href="#"
                                            class="text-2xl font-bold text-gray-700 hover:underline">
                                            <%=blogPost.title%>
                                        </a>
                                        <p class="mt-2 text-gray-600">
                                            <%=blogPost.description%>
                                        </p>
                                    </div>
                                    <div class="flex items-center justify-between mt-4">
                                        <!-- <a
                                            href="/blog/<%=blogPost._id%>" class="text-blue-500 hover:underline">Read
                                            more</a> -->
                                        <div>
                                            <h1 class="font-bold text-gray-700">
                                                <%=blogPost.author%>
                                            </h1>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <% }); %>
                    </div>
                </div>
            </div>
        </div>

        <%- include("partials/footer.ejs") %>
</body>

</html>